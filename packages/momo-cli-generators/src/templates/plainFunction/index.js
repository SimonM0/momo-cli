const index = require('./index.template');
const test = require('./test.template');
const plainFunction = require('./plainFunction.template');

module.exports = {
  index,
  test,
  plainFunction,
};
