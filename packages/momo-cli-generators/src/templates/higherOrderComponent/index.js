const index = require('./index.template');
const test = require('./test.template');
const higherOrderComponent = require('./higherOrderComponent.template');

module.exports = {
  index,
  test,
  higherOrderComponent,
};
