const containerTemplate = functionName =>
  `import { connect } from 'react-redux';

/**
 * This is a dummy component generated by momo cli. You need to replace this
 * with the component you want to wrap
 */
const DummyComponent = () => null;

export const mapStateToProps = (state, ownProps) => ({});

export const mapDispatchToProps = (dispatch, ownProps) => ({});

export const ${functionName} = connect(
  mapStateToProps,
  mapDispatchToProps,
)(DummyComponent);
`;

module.exports = containerTemplate;
